#!/bin/sh
set -e
# Automatically added by dh_pycentral
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall python-twisted-news
	if grep -qs '^python-twisted-news$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^python-twisted-news$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
